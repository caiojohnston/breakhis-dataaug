# Config smoke test — valida fix NaN, nao para producao
vae_lr: 1.0e-5
vae_epochs: 1
vae_batch_size: 2
kl_weight: 1.0e-4

ldm_lr: 1.0e-4
ldm_epochs: 3
ldm_batch_size: 8
latent_channels: 4
image_size: 256
num_classes: 8
embedding_dim: 512
timesteps: 1000

guidance_scale: 7.5
gen_batch_size: 8
